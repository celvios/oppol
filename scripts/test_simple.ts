import { ethers } from 'ethers';\nimport dotenv from 'dotenv';\n\ndotenv.config();\n\nasync function testContract() {\n    const provider = new ethers.JsonRpcProvider('https://bsc-testnet-rpc.publicnode.com');\n    const MARKET_ADDRESS = '0xB6a211822649a61163b94cf46e6fCE46119D3E1b';\n    \n    console.log('Contract Address:', MARKET_ADDRESS);\n    \n    const abi = [\n        'function getAllPrices(uint256) view returns (uint256[])'\n    ];\n    \n    const contract = new ethers.Contract(MARKET_ADDRESS, abi, provider);\n    \n    try {\n        const prices = await contract.getAllPrices(8);\n        console.log('Market 8 prices:', prices.map(p => Number(p) / 100));\n    } catch (e) {\n        console.log('Error:', e.message);\n    }\n}\n\ntestContract();