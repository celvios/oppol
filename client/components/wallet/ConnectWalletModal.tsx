"use client";

// Wallet connection removed as per user request
export default function ConnectWalletModal({
    isOpen,
    onClose,
    onConnect,
    context,
    contextData
}: any) {
    return null;
}

